version: '1'

services:
  microservices:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - spring.datasource.url=jdbc:oracle:thin:@fullstack_tp?TNS_ADMIN=/app/wallet/
      - spring.datasource.username=fullstack1
      - spring.datasource.password=ClaseDuoc123-
    restart: always